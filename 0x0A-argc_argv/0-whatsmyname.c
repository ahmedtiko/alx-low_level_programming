#include "main.h"

/**
 * main - main function
 * @argc: Input parameter
 * @argv[]: Input array
 * Return: 0 when successful
 */

int main(int argc, char *argv[])
{
	if (argc > 0)
	{
		printf("%s\n", argv[0]);
	}

	return (0);
}
